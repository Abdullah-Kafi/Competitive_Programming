///In the name of ALLAH, The Most Gracious and The Most Merciful
///Praise be to ALLAH

///Class-12: Bitwise Operations & ACM ICPC Preliminary Contests Easiest Problem

#include<stdio.h>

int main() {

    ///Bitwise Operation::
    ///Bitwise OR( | )::
//    int a, b, c;
//    scanf("%d %d %d", &a, &b, &c);
//
//    int OR_result = ( a | b | c);
//    printf("%d | %d | %d = %d\n", a, b, c, OR_result);

    ///Bitwise AND( & )::
//    int a, b, c;
//    scanf("%d %d %d", &a, &b, &c);
//
//    int AND_result = ( a & b & c);
//    printf("%d & %d & %d = %d\n", a, b, c, AND_result);

    ///Bitwise XOR( ^ )::
//    int a, b, c;
//    scanf("%d %d %d", &a, &b, &c);
//    int XOR_result = ( a ^ b ^ c);
//    printf("%d ^ %d ^ %d = %d\n", a, b, c, XOR_result);
    ///The XOR of "even" number of 1 is -> 0
    ///The XOR of "odd" number of 1 is -> 1
    ///The XOR of two "equal" value is -> 0 ( such that, a ^ a = 0; where a is integer )
    ///The XOR of "odd" number of "equal" value is -> that value ( such that, a ^ a ^ a = 0 ^ a = a; where a is integer )
    /// 0 ^ a = a

//    int a, b, c, d, e;
//    scanf("%d %d %d %d %d", &a, &b, &c, &d, &e); /// any two value are same, also another two value are same
//
//    int difValue = a ^ b ^ c ^ d ^ e;
//
//    printf("%d\n", difValue);
    ///End of The Class!

    return 0;
}

