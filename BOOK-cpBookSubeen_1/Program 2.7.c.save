#include<stdio.h>

int main()
{
    int n;
    double x;

    x = 10.8;
    n = (int) x;

    printf("Value of n is %d\n", n);
    printf("Value of x is %lf\n", x);
    
    double n = (int) 

    return 0;
}
