#include <stdio.h>

int main()
{
    int n = 1;

    while(n <= 100) {
        printf("%d\n", n);
        n++;

        if(n > 10) {
            printf("I am going to break the While loop.\n");
            break;
        }
    }

    printf("While loop breaked Successfully\n");

    return 0;
}
