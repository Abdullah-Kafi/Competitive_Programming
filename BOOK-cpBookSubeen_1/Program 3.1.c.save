#include <stdio.h>

int main()
{
    int n;

    n = 99.99;

//    printf("%d \n", n);

    if(condition) {
        //this code blocks will execute only and if only the condition is true.
    }
    else {
        
    }


    if(n >= 0) {
        printf("The number is positive. \n");
    }
    else {
        printf("The number is negative. \n");
    }

    return 0;
}
