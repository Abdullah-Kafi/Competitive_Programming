#include <stdio.h>

int main() {
    int n = 1;

    while(n < 10) {
        n++;

        if(n % 2 == 0) {
            printf("I won't print: %d. HaHa\n", n);
            continue;
        }
        printf("%d\n", n);
    }
    printf("While loop ended successfully.\n");

    return 0;
}
