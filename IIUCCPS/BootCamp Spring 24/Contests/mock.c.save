///In the name of ALLAH, The Most Gracious and The Most Merciful
///Praise be to ALLAH

#include<stdio.h>

int main()
{
    #ifndef ONLINE_JUDGE
        freopen("input.txt", "r", stdin);
    #endif

    int t, n, ar[1000001], dig[1000001];
    scanf("%d", &n);
    while(t--)
    {
        scanf("%d", &n);
        for(int i = 0; i < n; i++)
        {
            scanf("%d", &ar[i]);
        }
        
        for(int i = 0; i < n; i++)
        {
            sum += ar[i];
        }
        
    }
    return 0;
}
