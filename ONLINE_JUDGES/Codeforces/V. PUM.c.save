///In the name of ALLAH, The Most Gracious and The Most Merciful
///Praise be to ALLAH

#include<stdio.h>

int main() {
    int n, a = 1, b = 2, c =3;
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        printf("%d %d %d PUM\n", a++, b++, c++);
    }
    return 0;
}
