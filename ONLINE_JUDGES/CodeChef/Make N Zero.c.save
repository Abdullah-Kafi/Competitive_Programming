///In the name of ALLAH, The Most Gracious and The Most Merciful
///Praise be to ALLAH

#include<stdio.h>

int main() {
    int n, t;
    scanf("%d", &t);
    while(t--) {
        scanf("%d", &n);
        while(1) {
            if( ( ( n - 3 ) == 0 ) || ( ( n - 4 ) == 0 ) ) {
                printf("YES\n");
                break;
            }
//            n -= 4;
//            if( ( n - 4 ) == 0 ) {
//                printf("YES\n");
//                break;
//            }
//            n -= 3;
//            if(n < 0) {
//                printf("NO\n");
//                break;
//            }
        }
    }
    return 0;
}

