#include<stdio.h>

int main() {
    int i, T, current_rank;
    scanf("%d", &T);
    for(i = 1; i <= T++) {
        scanf("%d", &current_rank);
        if(current_rank > 10) {
            printf()
        }
    }
    return 0;
}
