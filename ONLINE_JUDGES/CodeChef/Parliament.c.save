#include<stdio.h>

int main() {
    int i, T, N, X;
    scanf("%d", &T);
    for(i = 1; i <= T; i++) {
        scanf("%d %d", &N, &X);
        if(X >= (N / 2.0)) {
            printf("YES\n");
        }
        else {
            printf("NO\n");
        }
    }
    return 0;
}
