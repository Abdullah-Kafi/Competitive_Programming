#incluude<stdio.h>

int main() {
    int i, T, A, B, C, D, E;
    scanf("%d");
    for(i = 1; i <= T; i++) {
        scanf("%d %d %d %d %d", &A, &B, &B, &C, &D);
        if((A + B) <= D && C <= E) {
            printf("YES\n");
        }
    }
    return 0;
}
