#include<bits/stdc++.h>
using namespace std;

int main()
{
    /**

    ///prints odd number from 1 to 10:
    for(int i = 1; i <= 10; i++)
    {
        ///if even continue the loop: don't go down
        if(i % 2 == 0)
            continue;
        cout << i << ' ';
    }
    cout << '\n';

    ///prints only 1, 2, and 3:
    for(int i = 1; i <= 10; i++)
    {
        ///if greater than 3, break the loop:
        if(i > 3) break;
        cout << i << ' ';
    }
    cout << '\n';

    ///takes input until the input is 0
    int a;
    while(scanf("%d", &a) != 0)
    {
        ///if the input is 0 break the loop
        if(a == 0) break;
        cout << a << '\n';
    }
    ///or, in short:
    while(scanf("%d", &a) != EOF && a)
        cout << a << '\n';

    */



    return 0;
}
