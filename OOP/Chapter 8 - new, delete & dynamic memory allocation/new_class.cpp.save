#include<iostream>
#include<string.h>

using namespace std;

class Student
{
public:
    string name;
    string id;
    string dept;
    double cgpa;
};

int main()
{
    Student* ptr;

    ptr = new Student; /// allocate Student type memory and return pointer to ptr

    ptr ->name = "Abdullah Kafi";
    ptr ->id = "C241036";
    ptr ->dept = "CSE";
    ptr -> cgpa = 3.542;

    cout << "Name: " << ptr -> name << endl;
    cout << "ID: " << ptr -> id << endl;
    cout << "Department: " << ptr -> dept << endl;
    cout << "CGPA: " << ptr -> cgpa << endl;

    return 0;
}
