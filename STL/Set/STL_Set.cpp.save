#include<bits/stdc++.h>
using namespace std;

int main()
{
//    set<int> st = {6, 10, 11, 11, 19};

    /// defining empty set
//    set<char> c;

//    c.insert('A');
//    c.insert('K');
//    c.insert('C');
    /// by default set is sorted in ascending order
//    for(auto u : c) cout << u << " ";
//    cout << endl;


    /// set< data_type, greater<data_type> > s; (for sorting in descending order)

//    set<int, greater<int>> s;
//
//    s.insert(10);
//    s.insert(15);
//    s.insert(5);
//    s.insert(70);
//
//    for(auto u : s) cout << u << " ";

//    set<int> num = {10, 20, 30, 50};
//
//
//    for(auto it = num.begin(); it != num.end(); it++) cout << *it << " ";
//    cout << endl;
//    cout << num.size() << endl;
//    cout << *num.begin() << endl; // 10
//    cout << *(--num.end()) << endl; // 50

//    set<int, greater<int>> s1;
//
//    s1.insert(10);
//    s1.insert(40);
//    s1.insert(30);
//    s1.insert(60);
//    s1.insert(20);
//    s1.insert(50);
//    s1.insert(50);
//    cout << "\nSet s1 elements: ";
//    for(auto it = s1.begin(); it != s1.end(); it++) cout << *it << " ";
//    cout << endl;
//
    /// assigning the elements from set s1 to set s2
//    set<int> s2(s1.begin(), s1.end());
//
//    set<int> :: iterator it;
//    cout << "\nSet s2 elements: ";
//    for(it = s2.begin(); it != s2.end(); it++) cout << *it << " ";
//    cout << endl;
//
//    s2.erase(s2.begin(), s2.find(30));
//    cout << "\nAfter erasing the elements less than 30: ";
//    for(it = s2.begin(); it != s2.end(); it++) cout << *it << " ";
//    cout << endl;

    /// Summation of Set elements::
//    set<int> numbers {1, 5, 6, 3, 9, 2};
//    int sum = 0;
//    while(!numbers.empty())
//    {
//        sum += *numbers.begin();
//        numbers.erase(numbers.begin());
//    }
//    cout << sum << endl;

    /// C++ Program to illustrate how to use
    /// set::insert() to insert the value near
    /// the specific position
//    set<int> num{5, 10, 18};
//    auto it = num.begin();
//    num.insert(it, 200);
//    /// Insert multiple elements
//    num.insert({1, 2, 3, 5, 5, 7});
//    for(auto u : num) cout << u << " ";

    /// Given n elements, print the unique element::
//    int n;
//    cout << "Enter no. of elements: ";
//    cin >> n;
//    vector<int> v(n);
//    cout << "Enter each elements: ";
//    for(int i = 0; i < n; i++) cin >> v[i];
//    set<int> st;
//    st.insert(v.begin(), v.end());
//    cout << "Unique elements are: ";
//    for(auto i : st) cout << i << " ";

    /// std::set::lower_bound() method:: set_name.lower_bound(value);
    set<int> s{1, 2, 3, 29, 3, 4, 7};
    auto it = s.lower_bound(4);
    if(it != s.end()) cout << *it << endl;
    else cout << "Lower bound not found" << endl;

    return 0;
}
